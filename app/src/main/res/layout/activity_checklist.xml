<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/app_background"
    tools:context="stoneframe.serena.gui.checklists.ChecklistActivity">

    <TextView
        android:id="@+id/checklistNameTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="16dp"
        android:paddingTop="16dp"
        android:paddingRight="16dp"
        android:paddingBottom="8dp"
        android:text="Test"
        android:textAppearance="@style/TextAppearance.Serena.ScreenTitle"
        android:textStyle="bold" />

    <ListView
        android:id="@+id/checklistItemView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/editChecklistButton"
        android:layout_below="@+id/checklistNameTextView"
        android:padding="8dp" />

    <Button
        android:id="@+id/editChecklistButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="16dp"
        android:layout_marginBottom="16dp"
        android:text="Edit..." />

    <Button
        android:id="@+id/resetButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="@dimen/space_sm"
        android:layout_marginBottom="16dp"
        android:layout_toStartOf="@+id/doneButton"
        android:text="Reset" />

    <Button
        style="@style/Widget.Serena.Button.Primary"
        android:id="@+id/doneButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentBottom="true"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:text="Done" />

</RelativeLayout>
